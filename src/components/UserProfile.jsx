import { Modal } from "bootstrap";
import Form from "react-bootstrap/Form";
import styles from "./UserProfile.module.css";

const UserProfile = ({ item, listArr }) => {
  return <div></div>;
};

export default UserProfile;
